# Copy this file to `.env` on the server (NOT in git) and fill in real
# values. See config/mail.php for how these are read, and
# docs/migration-requirements.md #19 for why mail() alone isn't enough.
#
# andrew@cleangreenturf.com is a Google Workspace address, so this sends
# via Gmail's SMTP relay using that same mailbox — leads land directly in
# the inbox you already check, sent from your own address.
#
# Setup (one-time, takes ~2 minutes):
#   1. While logged into andrew@cleangreenturf.com, go to
#      https://myaccount.google.com/security and turn on 2-Step
#      Verification if it isn't already on (required for step 2).
#   2. Go to https://myaccount.google.com/apppasswords, create a new app
#      password (name it something like "Clean Green Turf Website"), and
#      copy the 16-character password it generates.
#   3. Paste that as SMTP_PASSWORD below (remove any spaces Google shows
#      it with). This is NOT your normal Google login password — app
#      passwords are single-purpose and can be revoked independently.

SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=andrew@cleangreenturf.com
SMTP_PASSWORD=
SMTP_ENCRYPTION=tls
SMTP_FROM_EMAIL=andrew@cleangreenturf.com
SMTP_FROM_NAME=Clean Green Turf Website

# ---------------------------------------------------------------------------
# Admin dashboard password (protects /admin/appointments.php — the list of
# turf-installation-estimate bookings). Pick any strong password; there's no
# username, just this one shared password. See config/mail.php's env() for
# how it's read.
ADMIN_PASSWORD=

# ---------------------------------------------------------------------------
# Twilio (SMS) — used only for the day-before appointment-reminder text on
# /schedule-turf-installation-estimate bookings (see bin/send-reminders.php).
# Without these set, the scheduler still works fully (booking, email
# confirmations, rescheduling) — it just skips the reminder text until this
# is configured.
#
# Setup (~5 minutes, at https://console.twilio.com):
#   1. Sign up for Twilio (or use an existing account).
#   2. Buy/verify a phone number capable of sending SMS (a trial account can
#      send to your own verified number for free to test).
#   3. Copy the Account SID and Auth Token from the Console dashboard home
#      page, and the SMS-capable number you bought (in E.164 format, e.g.
#      +14695551234), into the three values below.
#   4. On Hostinger, set up an HOURLY cron job (hPanel > Advanced > Cron
#      Jobs) running `php bin/send-reminders.php` — see that file's header
#      comment for the exact command and why it needs to run hourly, not
#      once a day.
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_FROM_NUMBER=

# ---------------------------------------------------------------------------
# Zoho CRM — pushes new quote-form and turf-installation-scheduler leads
# into Zoho CRM's Deals module, into whichever pipeline (Turf Cleaning /
# Turf Installation / Turf Repair) matches the service requested. Without
# these set, the site works exactly as before — forms/handle-quote.php and
# scheduler/book.php just skip the CRM push (see includes/zoho-crm.php).
#
# Setup (~5 minutes, at https://api-console.zoho.com — sign in as
# andrew@cleangreenturf.com):
#   1. Click "Add Client" > "Self Client". This is the simplest option for
#      a server-to-server integration like this one — no redirect URL or
#      browser login flow needed, unlike "Server-based Applications".
#   2. In the Self Client screen, go to the "Generate Code" tab. For scope,
#      enter (all on one line, comma-separated, no spaces around commas):
#        ZohoCRM.modules.deals.ALL,ZohoCRM.modules.accounts.ALL,ZohoCRM.modules.contacts.ALL
#      (Deals alone isn't enough — creating a Deal also needs to look up
#      or create its linked Account and Contact records, confirmed by a
#      live MANDATORY_NOT_FOUND rejection when only deals.CREATE was
#      granted; see docs/audit-findings.md "Zoho CRM integration.")
#      Set duration to 10 minutes and add any description. Click Create,
#      then copy the "Code" it shows you — this expires fast, so do step 3
#      right away. If you already generated a code with just
#      ZohoCRM.modules.deals.CREATE, generate a new one with this wider
#      scope and redo steps 3-4 — the refresh token is tied to whatever
#      scope was granted when it was created.
#   3. Also on that same Self Client screen, note the Client ID and Client
#      Secret (under the "Client Secret" tab) — paste them into
#      ZOHO_CLIENT_ID / ZOHO_CLIENT_SECRET below.
#   4. Exchange the code for a refresh token — from any terminal (this can
#      be done from a personal computer, it doesn't need to run on the
#      server):
#        curl -X POST https://accounts.zoho.com/oauth/v2/token \
#          -d "grant_type=authorization_code" \
#          -d "client_id=YOUR_CLIENT_ID" \
#          -d "client_secret=YOUR_CLIENT_SECRET" \
#          -d "code=YOUR_CODE_FROM_STEP_2"
#      The response is JSON containing a "refresh_token" — copy that into
#      ZOHO_REFRESH_TOKEN below. This token doesn't expire, so this is a
#      one-time setup (unless it's ever revoked in Zoho's API Console).
#   5. If your Zoho account is on a data center other than the US
#      (zoho.com), e.g. zoho.eu or zoho.in, also set ZOHO_ACCOUNTS_DOMAIN
#      and ZOHO_API_DOMAIN to match (https://accounts.zoho.eu and
#      https://www.zohoapis.eu, for example) — leave them unset for zoho.com.
ZOHO_CLIENT_ID=
ZOHO_CLIENT_SECRET=
ZOHO_REFRESH_TOKEN=
ZOHO_ACCOUNTS_DOMAIN=
ZOHO_API_DOMAIN=

# ---------------------------------------------------------------------------
# Google Calendar sync — two things, both via includes/google-calendar.php
# (see docs/audit-findings.md "Google Calendar sync" and "Dynamic
# availability against Google Calendar"):
#   1. Creates/updates/deletes an event on a dedicated Google Calendar for
#      every turf-installation estimate booked through
#      /schedule-turf-installation-estimate.
#   2. Checks one or more calendars for existing busy time (a manually
#      added block, a personal appointment, anything already scheduled)
#      so the booking widget never offers a slot that actually conflicts
#      with something on your calendar(s).
# Without these set, the scheduler works exactly as before — booking,
# email confirmations, rescheduling, Zoho — it just skips both of the
# above (no calendar event created, and availability only reflects other
# website bookings, not anything else on your calendar).
#
# This uses a Google service account (server-to-server, no interactive
# login/consent screen, no refresh token to babysit).
#
# Two ways to give it calendar access — try #1 first:
#
# OPTION 1 (recommended): Domain-wide delegation. The service account
# acts AS your own Google Workspace account, so it automatically gets
# YOUR access to every calendar you own or have been shared — no sharing
# any individual calendar at all. This is Google's own recommended
# pattern for a script/service account working with Workspace Calendar,
# and it sidesteps a real snag OPTION 2 can run into: some Workspace
# "external sharing" org policies block or cap a non-domain principal
# (which a service account counts as) at read-only access no matter what
# permission you pick when sharing, and changing that org policy can take
# a long time to actually propagate.
#   1. In Cloud Console, open the service account (IAM & Admin > Service
#      Accounts > your account) and copy its "Unique ID" (a long number) —
#      also in the downloaded JSON key as "client_id".
#   2. In the Workspace Admin console (admin.google.com) > Security >
#      Access and data control > API controls > Domain-wide Delegation >
#      "Add new".
#   3. Paste the Unique ID as the Client ID. For OAuth scopes, enter:
#        https://www.googleapis.com/auth/calendar
#      Click Authorize.
#   4. Set GOOGLE_IMPERSONATE_EMAIL below to your own Workspace address
#      (e.g. andrew@cleangreenturf.com). GOOGLE_CALENDAR_ID and
#      GOOGLE_BUSY_CALENDAR_IDS still work exactly as described below —
#      just no per-calendar sharing step needed first, since impersonating
#      you already grants access to any calendar you can see.
#
# OPTION 2: Share each calendar individually with the service account's
# own email (no domain-wide delegation, no Workspace admin access
# needed) — leave GOOGLE_IMPERSONATE_EMAIL blank to use this instead. In
# Google Calendar (calendar.google.com), for the calendar you want
# bookings to appear ON: open its Settings (gear icon) > click that
# calendar's name under "Settings for my calendars" > "Share with
# specific people or groups" > "Add people" > paste the service account's
# client_email > permission "Make changes to events" > Send. If that
# permission option is greyed out, that's the external-sharing org-policy
# issue above — switch to Option 1 rather than fighting it.
#
# Setup (~5-10 minutes, at https://console.cloud.google.com):
#   1. Create a project (or use an existing one), then in "APIs & Services
#      > Library" enable the "Google Calendar API".
#   2. Go to "APIs & Services > Credentials" > "Create Credentials" >
#      "Service Account". Name it anything (e.g. "cleangreenturf-scheduler")
#      and finish the wizard — no roles/access needed at the project level.
#   3. Click into the new service account > "Keys" tab > "Add Key" >
#      "Create new key" > JSON. This downloads a .json file — open it in a
#      text editor, you'll need two fields from it:
#        - "client_email" — paste as-is into GOOGLE_SERVICE_ACCOUNT_EMAIL.
#        - "private_key" — copy the ENTIRE value between the quotes
#          (including the literal \n sequences, the -----BEGIN/END PRIVATE
#          KEY----- lines, all of it, still on one line) into
#          GOOGLE_SERVICE_ACCOUNT_PRIVATE_KEY below, wrapped in double
#          quotes. config/google-calendar.php converts those \n sequences
#          back into real newlines — don't retype or reformat the key.
#   4. Do Option 1 or Option 2 above to grant calendar access.
#   5. Get GOOGLE_CALENDAR_ID: on the calendar you want bookings to
#      appear ON, open its Settings > scroll to "Integrate calendar" >
#      copy the "Calendar ID" (a long string ending in
#      @group.calendar.google.com, or your own email for a primary
#      calendar under Option 1).
#   6. For any OTHER calendar that should just be checked for conflicts
#      (not necessarily where bookings get created): under Option 1,
#      nothing further needed if you own it — just add its Calendar ID to
#      GOOGLE_BUSY_CALENDAR_IDS below. Under Option 2, share it too (read
#      only / "See only free/busy" is enough for a busy-check-only
#      calendar). Comma-separated, no spaces. Leave blank to only check
#      GOOGLE_CALENDAR_ID itself.
GOOGLE_IMPERSONATE_EMAIL=
GOOGLE_SERVICE_ACCOUNT_EMAIL=
GOOGLE_SERVICE_ACCOUNT_PRIVATE_KEY=
GOOGLE_CALENDAR_ID=
GOOGLE_BUSY_CALENDAR_IDS=
